module.exports = {
	darkMode: "class", // Enable dark mode
	content: ["./src/**/*.{js,ts,jsx,tsx}"], // Scan all files in the src folder
	theme: {
		extend: {
			colors: {
				primary: "#0070f3" // Add custom colors if needed
			}
		}
	},
	plugins: []
};
